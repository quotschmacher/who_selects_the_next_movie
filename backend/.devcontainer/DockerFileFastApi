FROM python:3.12-slim

ENV PATH=$PATH:/root/.local/bin

# Install needed programs
#RUN apt-get -qq update && apt-get -qq upgrade
#RUN apt-get -qq install git -y > /dev/null

# Update pip
RUN pip install --upgrade pip

CMD [ "sleep", "infinity" ]